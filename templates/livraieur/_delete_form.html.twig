<form method="post" action="{{ path('livraieur_delete', {'id': livraieur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livraieur.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
