{% extends 'base_back.html.twig' %}

{% block title %}Livraison{% endblock %}

{% block body %}
    <h1>Livraison index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numl</th>

                <th>Dateliv</th>
                <th>Total</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for livraison in livraisons %}
            <tr>
                <td>{{ livraison.id }}</td>
                <td>{{ livraison.numl }}</td>
                <td>{{ livraison.Dateliv ? livraison.Dateliv|date('Y-m-d') : '' }}</td>
                <td>{{ livraison.total }}</td>
                

            <td>
                <a href="{{ path('livraison_show', {'id': livraison.id}) }}" class="btn btn-success">show</a>
                <a href="{{ path('livraison_edit', {'id': livraison.id}) }}"class="btn btn-danger">edit</a>
            </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="9">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('livraison_new') }}"class = "btn btn-success">Create new</a>
{% endblock %}
