{% extends 'base_back.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
    <h1>Produit</h1>

<br>

<td><img src ="{{ asset('uploads/images/' ~ produit.image) }}"
         alt =""  width="100" height="100"></td>
    <div class="border border-dark rounded p-2 clearfix">
<td>

    <table >

        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>Nom de produit</th>
                <td>{{ produit.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ produit.prix }}</td>
            </tr>

            <tr>
                <th>Stock</th>
                <td>{{ produit.stock }}</td>
            </tr>


        </tbody>
    </table>


    <a href="{{ path('produit_index') }}"class="btn-success">back to list</a>

    <a href="{{ path('produit_edit', {'id': produit.id}) }}"class="btn-danger">edit</a>
</td>
</div>


    {{ include('produit/_delete_form.html.twig') }}
{% endblock %}
